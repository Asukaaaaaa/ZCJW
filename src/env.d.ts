/// <reference types="@rsbuild/core/types" />

// declare global {
//   interface Window {
//     ace: AceAjax;
//   }
// }

type $TabItem = {
  name: string;
  id: string;
  active?: boolean;
  enabled?: boolean;
};
